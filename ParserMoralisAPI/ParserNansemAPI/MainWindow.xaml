<Window x:Class="ParserNansemAPI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParserNansemAPI"
        mc:Ignorable="d"
        Title="Moralis API" MaxHeight="500" MaxWidth="1000" MinHeight="500" MinWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Background="Peru">
            <Button x:Name="btnSettings" Content="Settings" Style="{StaticResource Menu}"/>
            <Button x:Name="btnParsing" Content="Parsing" Style="{StaticResource Menu}"/>
            <Button x:Name="btnBack" Content="Back" Style="{StaticResource Menu}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Background="FloralWhite">
            <Frame x:Name="mainFrame"/>
        </StackPanel>
    </Grid>
</Window>
